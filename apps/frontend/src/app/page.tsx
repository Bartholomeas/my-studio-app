import { Button } from "@/components/ui/button";

const Home = () => {
  return (
    <main className="relative">
      <main>
        <Button>Kliku</Button>
      </main>
    </main>
  );
};

export default Home;